/**
 * Web server domain - Express + REST API + static serving
 */

export { createAppServer as startServer } from "./server.js";
export type { ProjectInfo, ServerInstance, ServerOptions } from "./types.js";
